include<stdio.h>
int main()
{
	int a,b,i,rem;
	printf("Enter the value of min range:");
	scanf("%d",&a);
	printf("Enter the max range:");
	scanf("%d",&b);
	for(i=a;i<=b;i++)
	{
		rem=i%10;
		if(rem==3)
		continue;
		
		else
		printf("%d\n",i);
	
	}

	
	return 0;
